# Public Repository Rules

本目录是“陈佳敏律师 AI 工作台 / JiaminChen Legal Workbench”的公开仓库。

## 内容规则

- 只保留已经允许公开的代码、文档、模拟示例和必要资源；
- 禁止出现客户、事项、本机路径、凭证、内部 Wiki、律所未授权品牌资产和真实办案材料；
- 未发布产品必须标记“筹备中”或“拟开放”，不得提供虚假安装链接；
- 每个 Skill 的示例放在自己的 `examples/`，顶层 `showcase/` 只做精选索引；
- `solutions/` 是组合使用说明，不是独立安装包；
- 按项目单独授权：通用工具原则上 MIT，核心法律专业 Skills 原则上 CC BY-NC 4.0；每个项目必须保留自己的许可证文件，不设置一份根许可证覆盖全部内容；
- 第三方改造项目必须保留原版权和原许可证，并标注原项目、适配者及主要改动；
- `contract4` 公开副本的可见修订者和批注作者使用 `lawyer`，不得保留内部默认作者；
- `china-lawyer-letter-generator` 可以包含经用户确认允许上传的大成成都信头模板，但必须同时保留品牌与身份使用边界说明。

## 修改要求

修改产品说明前先核对对应 `SKILL.md` 和实际交付能力。新增或更新可安装内容时，同步维护根目录 `README.md`、产品 README、版本和 Release 说明。
