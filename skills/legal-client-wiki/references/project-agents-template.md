# 项目级 AGENTS.md 模板

项目级 `AGENTS.md` 应保持短小，只把新会话带到客户 Wiki 的运行入口：

```markdown
## 客户 Wiki

本项目维护客户级 Wiki。处理客户事项时，先读取 `wiki/规则.md`，再按其路由读取 `wiki/索引.md` 和本次相关页面。

交付文件完成不等于自动回写 Wiki。只有律师明确触发项目规则规定的 Wiki 收尾后，才准备回写提案。完整编译规则只维护在 `wiki/规则.md`，不在本文件重复。
```

如果已有项目规则，只追加客户 Wiki 入口，不覆盖其他任务路由、保密规则或工具指令。

